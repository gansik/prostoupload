

1. Installation
	Simply copy the /prostoupload/ directory into your TinyMCE plugins directory.

	tinyMCE.init({
		theme : "advanced",
		plugins : "...,prostoupload,...",
		...
		theme_advanced_buttons1 : "undo,redo...,prostoupload,...",
		...
		plugin_prostoupload_uploader : '/utils/prostoupload.asp',
		...
	});
